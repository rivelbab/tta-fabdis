{{ form_start(form, {'attr':{'class':'form-horizontal'}}) }}

    <div class="form-group">
        <label class="icon-primitive-square"> Nom du groupe </label>
        {{ form_widget(form.nom, {'attr':{'class':'form-control input-xlg border-primary'}}) }}
    </div>
    <div class="form-group">
        <label class="icon-primitive-square"> Fournisseur concerné </label>
        {{ form_widget(form.fournisseur, {'attr':{'class':'form-control input-xlg border-primary'}}) }}
    </div>
    <div class="form-group">
        <label class="icon-primitive-square"> Remise <small>(en pourcentage <i class="icon-percent"></i> )</small> </label>
        {{ form_widget(form.remise, {'attr':{'class':'form-control input-xlg border-primary'}}) }}
    </div>

    <div class="form-group pull-right">
        <br>
        <button type="submit" class="btn btn-primary"><i class="icon-user-plus"></i> {{ btn_label|default('Ajouter le groupe') }}</button>
    </div>
{{ form_end(form) }}
